# 📋 公共代码框架开发报告

## 一、本次变更摘要

| 项目 | 内容 |
|------|------|
| 变更文件 | `dev.html`（公共代码框架） |
| 变更类型 | 功能增强 |
| 变更描述 | 引入 `localStorage` 缓存机制，使页面刷新后自动恢复到上次访问的面板（标签 + 内容同步恢复） |
| 影响范围 | 导航切换、模式切换、页面启动初始化 |

### 新增的缓存 Key

| Key | 用途 | 写入时机 |
|-----|------|----------|
| `site_mode` | 简洁/全能模式 | 模式切换按钮点击 |
| `current_tab` | 当前面板 ID | 导航标签点击 + 模式切换 + 页面启动 |

---

## 二、公共代码保留注意事项（维护者必读）

### ✅ 1. `VALID_TABS` 白名单

js

const VALID_TABS = ['api','live','download','danmu','about'];


- **作用**：防止 `localStorage` 中存入脏数据导致面板无法显示
- **规则**：新增面板时必须同步加入此数组
- **读取时**：`VALID_TABS.includes(localStorage.getItem(TAB_KEY))` 做校验，非法值回退 `'api'`

### ✅ 2. `currentTabId` 初始化顺序

js

let currentTabId = VALID_TABS.includes(localStorage.getItem(TAB_KEY))

? localStorage.getItem(TAB_KEY)

: 'api';


- **必须在 `renderTabs()` 之前执行**
- 否则导航栏高亮和面板激活会不同步

### ✅ 3. 启动逻辑分支（核心修复点）

js

if (currentMode === 'simple') {

currentTabId = 'api';

localStorage.setItem(TAB_KEY, 'api');

renderApi();

} else {

showPanel(currentTabId);  // ✅ 同时激活标签 + 面板内容

}


- **之前的问题**：只调了 `renderTabs()` + `renderApi()`，导致标签高亮了但面板内容没切
- **修复**：全能模式必须走 `showPanel(currentTabId)` 而不是单独调 `renderApi()`

### ✅ 4. 模式切换时的缓存重置

js

// 简洁模式强制回点播

currentTabId = 'api';

localStorage.setItem(TAB_KEY, 'api');


- 简洁模式只显示点播，所以切回简洁时必须重置缓存
- 否则下次切回全能会跳到一个简洁模式下用户没见过的面板

### ✅ 5. `tabsData` 中的 `hideSearch` 字段

js

{id:"api",label:"点播",hideSearch:false},

{id:"download",label:"下载",hideSearch:true},


- 控制搜索框在对应面板是否显示
- 新增面板时**必须声明** `hideSearch`，否则搜索框可能意外显示/隐藏

### ✅ 6. CSS 简洁模式规则

css

body.simple-mode #live-panel,

body.simple-mode #download-panel,

body.simple-mode #danmu-panel,

body.simple-mode #about-panel { display:none !important; }


- 新增面板时必须同步添加一行 `body.simple-mode #xxx-panel { display:none !important; }`
- 否则简洁模式下新面板会泄露显示

---

## 三、开发分页注意事项（子页面开发者必读）

### 📌 1. 面板 HTML 结构规范

每个面板必须遵循以下结构：

html

<div class="tab-panel" id="xxx-panel">

<!-- 内容区域 -->

</div>


| 要求 | 说明 |
|------|------|
| `id` | 必须与 `tabsData` 中的 `id` 一致 |
| `class` | 必须有 `tab-panel`（默认 `display:none`） |
| 内容容器 | 建议有独立 `id`，方便搜索过滤和 JS 操作 |

### 📌 2. 新增面板的完整步骤（6 步缺一不可）

| 步骤 | 位置 | 操作 |
|------|------|------|
| ① | `tabsData` 数组 | 添加 `{id:"xxx", label:"显示名", hideSearch:true/false}` |
| ② | `VALID_TABS` 数组 | 添加 `'xxx'` |
| ③ | HTML 面板区 | 添加 `<div class="tab-panel" id="xxx-panel">...</div>` |
| ④ | CSS 简洁模式 | 添加 `body.simple-mode #xxx-panel { display:none !important; }` |
| ⑤ | `showPanel()` 函数 | 添加 `if(tabId==='xxx') renderXxx();` 加载调用 |
| ⑥ | `globalSearch()` 函数 | 添加该面板的搜索过滤逻辑（遍历 `data-name`） |

### 📌 3. 搜索功能扩展

公共代码中 `globalSearch()` 只处理了点播面板：

js

document.querySelectorAll('#api-panel .api-item').forEach(e=>{

const n=e.getAttribute('data-name')||'';

e.classList.toggle('hidden',!n.includes(kw));

});


子类页面需**自行追加**其他面板的过滤逻辑：

js

// 示例：直播面板

document.querySelectorAll('#live-panel .live-item').forEach(e=>{

const n=e.getAttribute('data-name')||'';

e.classList.toggle('hidden',!n.includes(kw));

});


> ⚠️ 切换面板时也要记得清除其他面板的 `hidden` 状态（参考 `showPanel()` 中的写法）

### 📌 4. 数据加载时机

| 面板 | 加载策略 | 说明 |
|------|----------|------|
| 点播 | `showPanel('api')` → `renderApi()` | 每次进入都重新渲染 |
| 直播 | `showPanel('live')` → `renderLiveList()` | 懒加载 |
| 弹幕 | `showPanel('danmu')` → `renderDanmu()` | 依赖 `loadConfig()` 先完成 |
| 下载 | `showPanel('download')` → `renderDownloads()` | 依赖 `loadConfig()` 先完成 |
| 关于 | 贡献者通过 `loadConfig()` 加载 | 启动后执行一次即可 |

**原则**：面板数据**懒加载**，切换到该面板时才请求，不要一次性全部加载。

### 📌 5. 复制按钮（无需额外代码）

公共代码已通过事件委托统一处理所有 `.copy-btn`：

js

document.addEventListener('click', function(e) {

const copyBtn = e.target.closest('.copy-btn');

if (!copyBtn) return;

// 自动读取 data-url 并复制

});


子类页面只需确保按钮上有 `data-url` 属性即可。

### 📌 6. localStorage 脏数据自动容错

- `VALID_TABS.includes()` 校验会自动回退到 `'api'`
- 无需手动清理用户浏览器缓存
- 未来如果重命名面板 ID，**必须**做迁移逻辑或接受用户缓存重置

---

## 四、已知限制 & 后续可优化项

| 项目 | 说明 | 优先级 |
|------|------|--------|
| 搜索框输入不记忆 | 刷新后搜索框清空，可考虑加 `search_keyword` 缓存 | 🟢 低 |
| 面板切换无动画 | 当前为瞬间切换，可加 `transition` / `slide` | 🟢 低 |
| 全能模式首次加载 | 非点播面板数据在首次切换时才加载，可预加载 | 🟡 中 |
| 多页面共享缓存冲突 | 如果未来多个 HTML 页面共用同一 `current_tab` key，需加前缀区分 | 🟡 中 |

---

## 五、整合后的公共代码（已含缓存逻辑）

> 完整 HTML 代码见上方对话中的代码块，此处仅列出与原始 `dev.html` 的**差异对比**。

### 差异清单

| 位置 | 原始代码 | 新增/修改 |
|------|----------|-----------|
| JS 常量区 | 无 `TAB_KEY` / `VALID_TABS` | ✅ 新增 |
| `currentTabId` 初始化 | `let currentTabId = 'api'` | ✅ 改为从 `localStorage` 读取 + 白名单校验 |
| `tabsData` | 无 `hideSearch` 字段 | ✅ 每个 tab 添加 `hideSearch` |
| `renderTabs()` 点击回调 | 只切换高亮 + `showPanel()` | ✅ 增加 `localStorage.setItem(TAB_KEY, currentTabId)` |
| `showPanel()` | 无搜索框显隐控制 | ✅ 根据 `hideSearch` 控制搜索框 `display` |
| 模式切换 `click` | 全能模式只调 `showPanel()` | ✅ 简洁模式增加缓存重置为 `'api'` |
| 启动代码 | `renderTabs(); renderApi();` | ✅ 改为 `if/else` 分支，全能模式调 `showPanel(currentTabId)` |
| CSS | 无新增 | ✅ 无（复用原有简洁模式规则） |

---

## 六、总结

> **公共代码框架现已具备「刷新记住面板」能力，标签和内容同步恢复，简洁模式行为不变。**
>
> 维护者新增面板时需同步修改 **6 个位置**（tabsData / VALID_TABS / HTML / CSS / showPanel / globalSearch），缺一不可。
>
> 子类页面开发者只需填充各面板的业务逻辑和渲染函数，框架层面的导航、缓存、复制、搜索显隐已统一处理。

